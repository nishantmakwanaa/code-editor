# Render Blueprint - Backend (server) only
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: code-editor-server
    runtime: node
    plan: free
    buildCommand: HUSKY=0 NODE_ENV=development pnpm install --frozen-lockfile
    startCommand: pnpm run start:server:render
